import React from "react";

const AboutPage = () => {
    return (
        <div>
            <h1>About</h1>
            <p>React, Redux, and React Router to the rescue!</p>
        </div>
    );
};

export default AboutPage;